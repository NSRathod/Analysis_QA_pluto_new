// here one can set parameters for program 
// 

{
  //gFBBA->SetKin3Body(160, 13, 13, 60);
  //Set no bins and max val in calib histos, 3rd value for max value in 4 and 5 det
  //gFBBA->SetDistPlane(300, 0, 140); 

  // Decide if TT tree should be filled or no (histos are writen any how)
  //gSymA->SetWriteTree(kTRUE);
  //gSymA->SetEnergy(150);  
}
